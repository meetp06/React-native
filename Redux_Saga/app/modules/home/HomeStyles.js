import {StyleSheet} from 'react-native';
// define your styles
const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  image: {
    height: 100,
    width: 100,
  },
});

export default styles;
