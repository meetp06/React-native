import { StyleSheet } from 'react-native';

const mainAppFooterStyle = StyleSheet.create({
    mainView: {
        alignItems: 'center',
        justifyContent: 'center',
        flexDirection: 'row',
    },

    iconText: {
        fontSize: 16,
        fontWeight: '400',
        color: 'black',
    },
});

export default mainAppFooterStyle;
