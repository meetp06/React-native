import { StyleSheet } from 'react-native';

const addPlayerModalStyle = StyleSheet.create({
    flex: {
        flex: 1,
    },

    bottomButton: {
        marginBottom: 7,
    },
});

export default addPlayerModalStyle;
